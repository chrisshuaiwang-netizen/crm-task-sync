# CRM-Task Sync 产品需求文档（PRD）

> **文档版本**：v2.0
> **产品名称**：CRM-Task Sync · 产品经理工作台
> **文档日期**：2026-03-30
> **状态**：已上线（GitHub Pages）

---

## 目录

1. [产品概述](#1-产品概述)
2. [目标用户与使用场景](#2-目标用户与使用场景)
3. [产品目标](#3-产品目标)
4. [整体架构](#4-整体架构)
5. [功能模块详细说明](#5-功能模块详细说明)
6. [数据模型](#6-数据模型)
7. [核心业务流程](#7-核心业务流程)
8. [AI 智能能力](#8-ai-智能能力)
9. [接口设计（RESTful API）](#9-接口设计restful-api)
10. [非功能性需求](#10-非功能性需求)
11. [技术架构](#11-技术架构)
12. [版本规划](#12-版本规划)

---

## 1. 产品概述

CRM-Task Sync 是面向 **To B 软件产品经理** 的一站式工作管理工具，解决 PM 在日常工作中客户需求分散、跟进状态不透明、工作计划难以聚焦的痛点。

产品将 **客户管理 → 需求收集 → 工作计划 → 日常记录** 整合为一条闭环工作流，同时内嵌 AI 辅助分析能力，帮助 PM 快速处理信息、聚焦优先级。

---

## 2. 目标用户与使用场景

### 2.1 目标用户

| 用户角色 | 描述 |
|---|---|
| To B 产品经理 | 负责多个企业客户的需求收集与跟进，需要管理多条产品线 |
| 产品总监 | 关注整体需求进度、客户分布、交付状态 |

### 2.2 典型使用场景

1. **需求收集**：客户通过微信、电话、会议提出需求，PM 在工具中快速录入，AI 自动打标签并评估优先级
2. **需求跟进**：需求从「未开始」逐步流转到「开发中」「已上线」，工作计划页自动同步最新状态
3. **每日工作规划**：打开工作台查看待处理需求和进行中需求，决定当天工作重心
4. **会议/沟通记录**：用日常事项模块记录会议纪要、客户沟通结论，AI 自动提取关键结论和行动项
5. **客户盘点**：按产品线查看客户列表，了解每个客户当前进行中的工作计划

---

## 3. 产品目标

### 3.1 核心目标

- **信息聚合**：客户、需求、工作计划、日常记录四类信息统一管理，无需跨工具切换
- **状态驱动**：需求状态变更自动触发工作计划更新，减少手动维护成本
- **AI 提效**：需求分析、摘要生成由 AI 辅助完成，降低 PM 记录与整理负担

### 3.2 成功指标（参考）

| 指标 | 目标值 |
|---|---|
| 需求录入时长 | < 2 分钟/条 |
| 需求状态可见性 | 100%（所有需求均有明确状态） |
| 日常记录覆盖率 | 每个工作日至少 1 条记录 |

---

## 4. 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     CRM-Task Sync                        │
├──────────┬──────────┬──────────┬──────────┬─────────────┤
│  总览     │  客户管理 │  需求管理 │  工作计划 │  日常事项    │
│ Dashboard │ Customers│  Inbox   │ Schedule │ DailyNotes  │
├──────────┴──────────┴──────────┴──────────┴─────────────┤
│                    Zustand Store（状态管理）               │
│          customers / requirements / dailyNotes           │
├─────────────────────────────────────────────────────────┤
│              LocalStorage 持久化（键：crm-task-sync）      │
└─────────────────────────────────────────────────────────┘
```

### 4.1 页面路由

| 路径 | 页面 | 说明 |
|---|---|---|
| `/#/` | Dashboard | 工作台总览 |
| `/#/customers` | Customers | 客户管理 |
| `/#/inbox` | Inbox | 需求管理 |
| `/#/schedule` | Schedule | 工作计划 |
| `/#/daily-notes` | DailyNotes | 日常事项 |

> 使用 HashRouter 以支持 GitHub Pages 静态部署，无需服务端路由配置。

### 4.2 数据关联关系

```
Customer (1) ──────────── (N) Requirement
                                  │
                    ┌─────────────┼──────────────┐
                    │             │              │
               [未开始]        [方案中]        [开发中]
                    │             │              │
             工作计划:        工作计划:       工作计划:
            待处理需求        产品方案        开发计划
```

---

## 5. 功能模块详细说明

### 5.1 总览/工作台（Dashboard）

**功能描述**：工作台首页，提供全局工作视角，快速了解当前状态分布与进行中工作。

#### 统计卡片（4 个）

| 卡片 | 数据来源 | 说明 |
|---|---|---|
| 待处理需求 | `requirements.filter(status === '未开始').length` | 尚未开始处理的需求总数 |
| 总需求数 | `requirements.length` | 全量需求数 |
| 客户总数 | `customers.length` | 已维护客户数 |
| 已上线需求 | `requirements.filter(status === '已上线').length` | 成功交付的需求数 |

#### 待处理需求列表

- **数据源**：`status === '未开始'` 的需求，最多展示 5 条
- **展示字段**：标题、优先级徽章、客户名称、标签（最多 2 个）、创建时间
- **交互**：点击任意条目跳转至 `/inbox`

#### 进行中需求列表

- **数据源**：`status ∈ {方案中, 待评审, 开发中}` 的需求，最多展示 5 条
- **排序规则**：
  1. 状态优先级：方案中（0）→ 待评审（1）→ 开发中（2）
  2. 同状态内按截止日期升序（无截止日期排最后）
- **展示字段**：标题、状态徽章、优先级徽章、截止日期
- **交互**：点击任意条目跳转至 `/schedule`

---

### 5.2 客户管理（Customers）

**功能描述**：管理 To B 客户基础信息，按产品线分组展示，支持快速查看关联需求和工作计划。

#### 客户列表

- 按**产品分类**分组展示，分组内按优先级排序（高 → 中 → 低）
- 筛选条件：产品分类、优先级、关键词（名称搜索）
- 每张卡片展示：客户名称、优先级指示点、行业标签、产品分类标签、联系人、需求总数

#### 客户详情面板（右侧滑出）

展示基本信息后，提供两个 Tab：

**「需求列表」Tab**
- 展示该客户所有需求（全状态）
- 字段：标题、状态徽章、优先级、产品分类标签、需求标签

**「工作计划」Tab**
- 数据源：该客户 `status ∈ {方案中, 开发中}` 的需求
- 字段：标题、状态徽章（方案中/开发中）、优先级、截止日期
- 与工作计划页数据自动联动，需求状态变更后实时更新

#### 新增/编辑客户表单

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| 客户名称 | text | ✅ | 公司全称 |
| 所属行业 | select | ✅ | 金融科技/大数据/零售电商/物流科技/医疗健康/教育科技/企业服务/其他 |
| 产品分类 | select | ✅ | 见产品分类枚举 |
| 优先级 | select | ✅ | 高/中/低 |
| 联系人 | text | ❌ | 主要对接人姓名 |
| 联系电话 | text | ❌ | 主要对接人电话 |
| 备注 | textarea | ❌ | 客户背景、特殊注意事项 |

#### 产品分类枚举

| 分类 | 颜色标识 |
|---|---|
| DAM | 蓝色（bg-blue-100） |
| 开放平台-国内标准版 | 紫色（bg-purple-100） |
| 开放平台-海外标准版 | 靛蓝色（bg-indigo-100） |
| 私有化部署 | 橙色（bg-orange-100） |
| B TO C合作 | 青色（bg-teal-100） |

---

### 5.3 需求管理（Inbox）

**功能描述**：需求全生命周期管理，从录入到上线的完整跟踪，是产品核心模块。

#### 需求列表

**状态筛选 Tab（顶部）**

| Tab | 显示内容 |
|---|---|
| 全部 | 所有需求，显示总数 |
| 未开始 | 已记录尚未启动的需求 |
| 方案中 | PM 正在制定方案的需求 |
| 待评审 | 等待开发团队评审的需求 |
| 开发中 | 研发已排期开发的需求 |
| 已上线 | 已发布到生产环境的需求 |
| 已暂停 | 暂时搁置的需求 |
| 已拒绝 | 不予实现的需求 |

每个 Tab 右侧显示该状态的需求数量。

**条件筛选（下拉组合）**
- 产品分类：DAM / 开放平台-国内标准版 / 开放平台-海外标准版 / 私有化部署 / B TO C合作
- 优先级：高 / 中 / 低
- 来源客户：所有已维护客户
- 支持多条件组合过滤；筛选激活时显示「清除筛选」按钮

**排序规则**：
1. 按状态优先级排序：未开始(0) → 方案中(1) → 待评审(2) → 开发中(3) → 已上线(4) → 已暂停(5) → 已拒绝(6)
2. 同状态内按创建时间降序（最新在前）

**需求卡片展示字段**：
- 状态指示点（颜色对应状态）+ 需求标题
- 来源客户名 · 需求来源渠道 · 产品分类标签 · 需求类型标签
- 需求内容摘要（单行截断）
- 状态徽章 + 优先级徽章 + 创建时间

#### 需求详情面板（右侧 384px 滑出）

分区展示以下内容：

1. **元数据区**：状态、优先级、产品分类、来源客户、需求来源渠道、截止日期、记录时间、标签
2. **需求内容**：完整原始描述（保留换行）
3. **AI 分析摘要**：蓝色卡片展示（仅有数据时显示）
4. **附件区**：文件 URL 链接（可点击跳转） + 文件名备注
5. **状态变更操作区**：7 个状态按钮，当前状态高亮，一键切换

#### 新增/编辑需求弹窗

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| 需求标题 | text | ✅ | 简短描述需求主题 |
| 产品分类 | select | ✅ | 选择客户后可自动填充 |
| 来源客户 | select | ❌ | 选择后自动带入该客户的产品分类 |
| 需求来源 | select | ❌ | 微信 / 会议 / 电话 / 邮件 |
| 需求内容 | textarea | ✅ | 详细需求描述，AI 分析的输入源 |
| 优先级 | radio | ✅ | 高 / 中 / 低；AI 分析后自动填充 |
| 需求标签 | checkbox | ❌ | 功能优化 / Bug修复 / 新需求 / 数据需求 / 体验优化；AI 分析后自动填充 |
| 截止日期 | date | ❌ | 期望交付时间 |
| 附件 URL | text | ❌ | 相关文档、原型或设计稿链接 |
| 附件文件名 | text | ❌ | 附件备注名称 |

> **AI 分析触发**：在需求内容填写完毕后，点击弹窗中的「AI 分析」按钮（带 Sparkles 图标），约 1.5 秒后自动填充标签、优先级、AI 摘要。

> **新建需求初始状态**：固定为「待评审」，由服务端控制，客户端不可指定。

#### 需求状态流转

```
未开始 ──→ 方案中 ──→ 待评审 ──→ 开发中 ──→ 已上线
   ↘           │                    │
              └──────→ 已暂停 ←────┘
              └──────→ 已拒绝
```

| 状态 | 业务含义 | 颜色标识 |
|---|---|---|
| 未开始 | 已记录，尚未启动分析或排期 | 灰色 |
| 方案中 | PM 正在制定产品方案 | 靛蓝色 |
| 待评审 | 方案待开发团队评审排期 | 黄色 |
| 开发中 | 研发已排期并开始编码 | 蓝色 |
| 已上线 | 功能已发布到生产环境 | 绿色 |
| 已暂停 | 因资源或优先级暂时搁置 | 灰色 |
| 已拒绝 | 评估后不予实现，已关闭 | 红色 |

---

### 5.4 工作计划（Schedule）

**功能描述**：以需求状态为数据源，自动聚合三类工作任务，无需手动录入，与需求管理实时双向联动。

#### Tab 结构

| Tab 名称 | 数据来源 | 进度记录字段 |
|---|---|---|
| 待处理需求 | `requirements[status === '未开始']` | `planNote`（备注） |
| 产品方案 | `requirements[status === '方案中']` | `planNote`（方案进度） |
| 开发计划 | `requirements[status === '开发中']` | `devNote`（开发进度） |

**排序规则（各 Tab 一致）**：
1. 优先级降序（高 → 中 → 低）
2. 同优先级按创建时间降序

**Tab 顶部提示**：
- 「来自需求管理中[状态]的需求，在需求管理中修改状态后自动同步」

#### 需求卡片（可展开/收起）

**折叠状态**（默认）：
- 左侧状态指示点 + 需求标题
- 优先级徽章 + 展开按钮
- 元数据行：来源客户、产品分类标签、截止日期、需求类型标签（最多 2 个）

**展开状态**（点击箭头按钮）：
- 需求内容全文
- AI 摘要（蓝色卡片，有数据时显示）
- 进度备注区：
  - 显示当前备注内容（或「暂无记录，点击编辑填写」）
  - 「编辑」按钮 → 展开 textarea（3 行）+ 保存/取消按钮
  - 保存调用 `updateRequirement(id, { planNote/devNote: value })`，保存后 Toast 提示

---

### 5.5 日常事项（DailyNotes）

**功能描述**：会议纪要、客户沟通记录、日常备忘的管理工具，富文本编辑，AI 智能总结。

#### 左侧列表面板（固定 288px）

- **顶部**：页面标题「日常事项」+ 「新建」按钮
- **搜索框**：关键词搜索标题和正文内容（实时过滤）
- **时间筛选**：全部 / 今天 / 本周 / 本月
- **分类筛选**：全部 / 会议 / 沟通 / 其他
- **事项列表**：按日期降序排列
  - 分类图标 + 标题 + 内容预览（纯文本，去除 HTML，最多 2 行）
  - 分类标签 + 日期
  - 当前选中项：蓝色背景 + 左边框高亮
- **底部**：总记录数

#### 右侧编辑面板

**元数据行**：
- 标题输入框（必填，大号字体）
- 日期选择器（默认今日）
- 分类选择器

**富文本工具栏**：

| 按钮 | 功能 | 快捷键 |
|---|---|---|
| **B** | 加粗 | Ctrl+B |
| *I* | 斜体 | Ctrl+I |
| ~~S~~ | 删除线 | — |
| H2 | 二级标题 | — |
| H3 | 三级标题 | — |
| 无序列表 | Bullet list | — |
| 有序列表 | Ordered list | — |
| 引用块 | Blockquote | — |
| 分隔线 | Horizontal rule | — |
| `{ }` | 代码块 | — |

**编辑区**：Tiptap 富文本编辑器，内容以 HTML 格式持久化存储，最小高度 280px

**AI 总结卡片**（生成后显示在编辑区下方）：
- 蓝色渐变背景卡片
- 标题「AI 总结」+ Sparkles 图标
- 总结文本内容
- 右上角关闭按钮（清空 aiSummary 并标记为未保存）

**底部操作栏**：
- 左侧：删除按钮（红色 hover，需二次确认弹窗）
- 右侧：「有未保存的修改」提示 + 「AI 总结」按钮 + 「保存」按钮（保存时同时存储 aiSummary）

#### 日常事项分类

| 分类 | 图标 | 颜色 | 使用场景 |
|---|---|---|---|
| 会议 | Users | 蓝色 | 内部会议、客户评审会、需求讨论 |
| 沟通 | MessageSquare | 紫色 | 电话沟通、微信沟通、1on1 |
| 其他 | StickyNote | 灰色 | 备忘录、灵感记录、待办事项 |

#### 新建/切换笔记逻辑

- 点击「新建」→ 左侧列表顶部出现「新建事项…」占位项 → 右侧编辑区清空
- 点击已有事项 → 右侧切换到该事项内容（Tiptap 编辑器通过 `editor.commands.setContent()` 重置，避免 React 受控/非受控冲突）
- 切换笔记时，所有本地状态（title/date/category/aiSummary/dirty）均重置

---

## 6. 数据模型

### 6.1 Customer（客户）

```typescript
interface Customer {
  id: string               // 唯一标识，格式：c{timestamp}
  name: string             // 客户名称（必填）
  industry: string         // 所属行业
  priority: '高' | '中' | '低'   // 客户重要程度
  contact: string          // 主要联系人姓名
  phone: string            // 联系人电话
  notes: string            // 备注信息
  productCategory: string  // 所属产品分类（必填）
  createdAt: string        // ISO 8601 创建时间
}
```

### 6.2 Requirement（需求）

```typescript
type RequirementStatus =
  | '未开始' | '方案中' | '待评审'
  | '开发中' | '已上线' | '已暂停' | '已拒绝'

type RequirementTag = '功能优化' | 'Bug修复' | '新需求' | '数据需求' | '体验优化'

interface Requirement {
  id: string               // 唯一标识，格式：r{timestamp}
  title: string            // 需求标题（必填）
  content: string          // 需求详细内容（必填）
  customerId: string       // 关联客户 ID
  productCategory: string  // 关联产品分类（必填）
  tags: RequirementTag[]   // 需求标签，可多选，最多 3 个
  priority: '高' | '中' | '低'   // 优先级
  status: RequirementStatus       // 当前状态
  source: string           // 需求来源渠道：微信/会议/电话/邮件
  createdAt: string        // ISO 8601 创建时间
  deadline: string         // 截止日期，格式 YYYY-MM-DD
  aiSummary: string        // AI 分析摘要
  planNote: string         // 方案进度备注（工作计划「产品方案」Tab 中编辑）
  devNote: string          // 开发进度备注（工作计划「开发计划」Tab 中编辑）
  fileUrl: string          // 附件文档链接 URL
  fileName: string         // 附件文件名备注
}
```

### 6.3 DailyNote（日常事项）

```typescript
type NoteCategory = '会议' | '沟通' | '其他'

interface DailyNote {
  id: string             // 唯一标识，格式：n{timestamp}
  title: string          // 事项标题（必填）
  content: string        // 富文本 HTML 内容
  date: string           // 事项日期，格式 YYYY-MM-DD
  category: NoteCategory // 事项分类
  aiSummary: string      // AI 总结文本
  createdAt: string      // ISO 8601 创建时间
}
```

---

## 7. 核心业务流程

### 7.1 需求全生命周期

```
客户提出需求（微信/会议/电话）
         │
         ▼
PM 在「需求管理」录入需求
  - 填写标题、内容、关联客户、产品分类
  - 点击「AI 分析」→ 自动填充标签、优先级、摘要
  - 初始状态：待评审
         │
         ▼
需求状态流转（PM 在详情面板操作）
  ├─ 改为「未开始」→ 进入工作计划「待处理需求」Tab
  ├─ 改为「方案中」→ 进入工作计划「产品方案」Tab（可填方案进度）
  ├─ 改为「待评审」→ 出现在总览「进行中需求」
  ├─ 改为「开发中」→ 进入工作计划「开发计划」Tab（可填开发进度）
  └─ 改为「已上线」→ 总览「已上线需求」计数 +1
```

### 7.2 客户-需求-工作计划联动

```
客户详情页（Customers）
  ├─「需求列表」→ 该客户所有需求（全状态展示）
  └─「工作计划」→ 该客户 status ∈ {方案中, 开发中} 的需求（自动关联）

工作计划页（Schedule）
  ├─「待处理需求」→ requirements[status=未开始]，按优先级+创建时间排序
  ├─「产品方案」  → requirements[status=方案中]，支持填写方案进度备注
  └─「开发计划」  → requirements[status=开发中]，支持填写开发进度备注

总览页（Dashboard）
  ├─ 待处理需求列表 → requirements[status=未开始]，最多 5 条
  └─ 进行中需求列表 → requirements[status ∈ {方案中,待评审,开发中}]
                       按状态顺序 + 截止时间排序，最多 5 条
```

### 7.3 日常事项流程

```
打开「日常事项」页面
  ├─ 新建：点击「新建」→ 填写标题/日期/分类 → 写入富文本内容
  │           → 可点击「AI 总结」生成摘要 → 点击「保存」持久化
  └─ 查看/编辑：左侧列表选中已有事项 → 右侧加载对应内容 → 编辑后保存
```

---

## 8. AI 智能能力

### 8.1 需求智能分析（analyzeRequirement）

**触发方式**：需求编辑弹窗中，填写内容后点击「AI 分析」按钮，约 1.5 秒返回结果

**输入**：需求内容（纯文本字符串）

**输出**：

| 字段 | 类型 | 说明 |
|---|---|---|
| tags | string[] | 识别出的需求标签，最多 3 个 |
| priority | '高' \| '中' \| '低' | 建议优先级 |
| summary | string | 内容摘要（前 60 字）+ 处置建议 |

**标签识别规则（关键词匹配）**：

| 标签 | 触发关键词（部分） |
|---|---|
| Bug修复 | bug、修复、崩溃、错误、异常、故障、不显示、无法、闪退 |
| 功能优化 | 优化、改进、升级、提升、完善、增强、支持、导出、导入 |
| 新需求 | 新增、新功能、增加、添加、开发新、希望、想要、功能请求 |
| 数据需求 | 数据、报表、统计、分析、看板、图表、报告、指标 |
| 体验优化 | 体验、交互、界面、UI、UX、加载慢、速度、响应、流畅 |

**优先级判断规则**：

| 优先级 | 触发关键词（部分） |
|---|---|
| 高 | 紧急、ASAP、影响收入、崩溃、无法使用、严重、立即、尽快、阻塞、故障 |
| 低 | 建议、希望、下季度、长期、有时间、可以考虑、未来、规划 |
| 中 | 其他情况（默认） |

**摘要生成逻辑**：`内容前 60 字 + " | " + 主标签对应的处置建议`

| 主标签 | 处置建议 |
|---|---|
| Bug修复 | 建议立即排查根因，安排紧急修复版本 |
| 功能优化 | 建议纳入下个迭代版本，评估改造成本 |
| 新需求 | 建议召开需求评审会，明确验收标准后排期 |
| 数据需求 | 建议与数据团队协同，明确数据来源与口径 |
| 体验优化 | 建议结合用户反馈数据，制定优化方案 |

---

### 8.2 日常事项智能总结（summarizeDailyNote）

**触发方式**：日常事项编辑页底部点击「AI 总结」按钮，约 900ms 后展示结果

**输入**：`{ title: string, content: string(HTML), category: string }`

**处理逻辑**：
1. 剥离 HTML 标签，提取纯文本
2. 检测三类信号：

| 信号类型 | 检测关键词（部分） |
|---|---|
| 行动项 | 确认、跟进、安排、完成、提交、联系、发送、准备、推进、处理、对接 |
| 决策结论 | 决定、确认、确定、同意、通过、结论、方案、明确、约定、达成 |
| 风险阻塞 | 风险、延期、阻塞、困难、待解决、未确认、不确定、问题 |

3. 按模板生成自然语言总结，包含：
   - 分类引言（会议/沟通/其他 对应不同措辞）
   - 内容摘要（前 80 字）
   - 决策提醒、行动项提醒、风险提醒（按检测结果选择性追加）
   - 无结论时追加默认建议

> **当前实现**：本地规则引擎（关键词匹配），适合轻量部署。
> **生产建议**：接入 Claude API（`claude-sonnet-4-6` 模型），用 Prompt 工程替代关键词匹配，大幅提升分析质量。

---

## 9. 接口设计（RESTful API）

> 以下为后端服务化时的标准 API 设计。当前版本使用 Zustand + LocalStorage 代替，所有 CRUD 操作均在前端完成。

### 9.0 基础规范

- **Base URL**：`https://api.crm-task.example.com/v1`
- **认证方式**：`Authorization: Bearer <JWT Token>`
- **响应格式**：
  ```json
  { "code": 200, "message": "ok", "data": {} }
  ```
- **时间格式**：ISO 8601（`2026-03-30T08:00:00Z`）
- **分页参数**：`page`（默认 1）、`pageSize`（默认 20）

---

### 9.1 客户管理接口（/customers）

#### 获取客户列表

```
GET /customers
```

**Query 参数**：

| 参数 | 类型 | 说明 |
|---|---|---|
| productCategory | string | 按产品分类筛选 |
| priority | string | 高 / 中 / 低 |
| search | string | 客户名称模糊搜索 |
| page | number | 页码 |
| pageSize | number | 每页数量 |

**响应示例**：
```json
{
  "code": 200,
  "data": {
    "total": 42,
    "list": [
      {
        "id": "c001",
        "name": "北京云信科技有限公司",
        "industry": "金融科技",
        "priority": "高",
        "contact": "张伟",
        "phone": "138-0010-1234",
        "notes": "核心大客户，年度合同额 500 万",
        "productCategory": "DAM",
        "createdAt": "2024-09-15T08:00:00Z",
        "requirementCount": 5,
        "activeReqCount": 2
      }
    ]
  }
}
```

#### 创建客户

```
POST /customers
Content-Type: application/json
```

**Request Body**：
```json
{
  "name": "客户名称",
  "industry": "金融科技",
  "priority": "高",
  "contact": "联系人姓名",
  "phone": "手机号",
  "notes": "备注信息",
  "productCategory": "DAM"
}
```

**响应**：
```json
{
  "code": 200,
  "data": { "id": "c1743300000000", ...customer }
}
```

#### 更新客户

```
PUT /customers/:id
Content-Type: application/json
```

Request Body 同创建，支持部分字段更新。

#### 删除客户

```
DELETE /customers/:id
```

> 删除前检查是否存在关联需求；若有，返回 409 并提示「该客户下存在 N 条需求，请先处理后再删除」。

---

### 9.2 需求管理接口（/requirements）

#### 获取需求列表

```
GET /requirements
```

**Query 参数**：

| 参数 | 类型 | 说明 |
|---|---|---|
| status | string | 状态筛选，多值用逗号分隔：`未开始,方案中` |
| priority | string | 高 / 中 / 低 |
| customerId | string | 按客户 ID 筛选 |
| productCategory | string | 按产品分类筛选 |
| sortBy | string | `status`（默认）/ `createdAt` / `deadline` |
| order | string | `asc` / `desc`（默认 desc） |
| page | number | 页码 |
| pageSize | number | 每页数量 |

**响应示例**：
```json
{
  "code": 200,
  "data": {
    "total": 88,
    "statusCounts": {
      "全部": 88,
      "未开始": 12,
      "方案中": 8,
      "待评审": 5,
      "开发中": 15,
      "已上线": 40,
      "已暂停": 4,
      "已拒绝": 4
    },
    "list": [
      {
        "id": "r001",
        "title": "报表导出支持 Excel 格式",
        "content": "目前系统只支持 CSV...",
        "customerId": "c001",
        "customerName": "北京云信科技有限公司",
        "productCategory": "DAM",
        "tags": ["功能优化", "数据需求"],
        "priority": "高",
        "status": "开发中",
        "source": "微信",
        "createdAt": "2026-03-10T09:15:00Z",
        "deadline": "2026-03-31",
        "aiSummary": "客户需要将报表导出格式从CSV升级为Excel...",
        "planNote": "已完成竞品调研",
        "devNote": "后端接口已完成，前端联调中",
        "fileUrl": "",
        "fileName": ""
      }
    ]
  }
}
```

#### 创建需求

```
POST /requirements
Content-Type: application/json
```

**Request Body**：
```json
{
  "title": "需求标题",
  "content": "详细需求描述",
  "customerId": "c001",
  "productCategory": "DAM",
  "tags": ["新需求"],
  "priority": "中",
  "source": "微信",
  "deadline": "2026-04-30",
  "aiSummary": "AI 生成的摘要",
  "fileUrl": "https://docs.example.com/xxx",
  "fileName": "需求文档.pdf"
}
```

> 服务端自动设置 `status = '待评审'`，`createdAt = now()`，`id` 自动生成。

**响应**：
```json
{ "code": 200, "data": { "id": "r1743300000000", ...requirement } }
```

#### 更新需求

```
PUT /requirements/:id
```

支持部分字段更新（PATCH 语义），字段范围同创建接口。

#### 更新需求状态

```
PATCH /requirements/:id/status
Content-Type: application/json
```

**Request Body**：
```json
{
  "status": "开发中",
  "remark": "需求评审通过，已排入本迭代"
}
```

**响应**：
```json
{ "code": 200, "data": { "id": "r001", "status": "开发中", "updatedAt": "..." } }
```

#### 更新进度备注

```
PATCH /requirements/:id/note
Content-Type: application/json
```

**Request Body**：
```json
{
  "field": "planNote",
  "value": "方案初稿已完成，本周五提交评审"
}
```

> `field` 枚举值：`planNote` / `devNote`

#### 删除需求

```
DELETE /requirements/:id
```

#### 需求统计（用于总览工作台）

```
GET /requirements/stats
```

**响应**：
```json
{
  "code": 200,
  "data": {
    "pending": 12,
    "active": 28,
    "online": 40,
    "total": 88
  }
}
```

---

### 9.3 工作计划接口

> 工作计划无独立数据表，数据直接从需求接口获取，通过 `status` 参数筛选。

#### 获取各 Tab 数据

```
GET /requirements?status=未开始&sortBy=priority    # 待处理需求 Tab
GET /requirements?status=方案中&sortBy=priority    # 产品方案 Tab
GET /requirements?status=开发中&sortBy=priority    # 开发计划 Tab
```

---

### 9.4 日常事项接口（/daily-notes）

#### 获取日常事项列表

```
GET /daily-notes
```

**Query 参数**：

| 参数 | 类型 | 说明 |
|---|---|---|
| category | string | 会议 / 沟通 / 其他 |
| dateStart | string | 开始日期，格式 YYYY-MM-DD |
| dateEnd | string | 结束日期，格式 YYYY-MM-DD |
| search | string | 搜索标题和内容（纯文本匹配） |
| page | number | 页码 |
| pageSize | number | 每页数量 |

**快捷日期范围参数**（优先于 dateStart/dateEnd）：

| 值 | 说明 |
|---|---|
| `today` | 今天 |
| `week` | 本周（周一至周日） |
| `month` | 本月 |

**响应示例**：
```json
{
  "code": 200,
  "data": {
    "total": 56,
    "list": [
      {
        "id": "n001",
        "title": "云信科技周例会",
        "content": "<p>讨论了权限模块排期...</p><ul><li>确认 4 月底上线</li></ul>",
        "contentPreview": "讨论了权限模块排期 确认 4 月底上线",
        "date": "2026-03-30",
        "category": "会议",
        "aiSummary": "本次会议已完成记录。已产生明确结论...",
        "createdAt": "2026-03-30T10:00:00Z"
      }
    ]
  }
}
```

#### 创建日常事项

```
POST /daily-notes
Content-Type: application/json
```

**Request Body**：
```json
{
  "title": "事项标题",
  "content": "<p>富文本 HTML 内容</p>",
  "date": "2026-03-30",
  "category": "会议",
  "aiSummary": ""
}
```

#### 更新日常事项

```
PUT /daily-notes/:id
```

字段范围同创建接口。

#### 删除日常事项

```
DELETE /daily-notes/:id
```

---

### 9.5 AI 接口（/ai）

#### 需求智能分析

```
POST /ai/analyze-requirement
Content-Type: application/json
```

**Request Body**：
```json
{
  "content": "需求详细内容文本（纯文本，不含 HTML）"
}
```

**响应**：
```json
{
  "code": 200,
  "data": {
    "tags": ["功能优化", "数据需求"],
    "priority": "高",
    "summary": "客户需要将报表导出格式从CSV升级为Excel... | 建议纳入下个迭代版本，评估改造成本。"
  }
}
```

> **生产实现建议**：调用 Claude API，Prompt 示例：
>
> ```
> 你是一位经验丰富的产品经理助手。请分析以下需求描述，以 JSON 格式返回：
> - tags: 需求类型标签（从[功能优化,Bug修复,新需求,数据需求,体验优化]中选最多3个）
> - priority: 优先级（高/中/低）
> - summary: 一句话核心摘要 + 处置建议（不超过100字）
>
> 需求内容：{content}
> ```

#### 日常事项智能总结

```
POST /ai/summarize-note
Content-Type: application/json
```

**Request Body**：
```json
{
  "title": "事项标题",
  "content": "<p>HTML 富文本内容</p>",
  "category": "会议"
}
```

**响应**：
```json
{
  "code": 200,
  "data": {
    "summary": "本次会议「云信科技周例会」已完成记录。内容摘要：讨论了权限模块排期... 已产生明确结论，建议及时同步相关干系人。存在待跟进事项，请及时处理。"
  }
}
```

---

### 9.6 错误码规范

| HTTP 状态码 | code | 说明 | 常见场景 |
|---|---|---|---|
| 200 | 200 | 成功 | 所有成功响应 |
| 400 | 400 | 参数错误 | 缺少必填字段、格式不合法 |
| 401 | 401 | 未认证 | Token 缺失或已过期 |
| 403 | 403 | 无权限 | 操作他人数据 |
| 404 | 404 | 资源不存在 | 查询的 ID 不存在 |
| 409 | 409 | 数据冲突 | 删除有关联数据的记录 |
| 422 | 422 | 业务校验失败 | 状态流转不合法 |
| 429 | 429 | 请求过于频繁 | 触发限流 |
| 500 | 500 | 服务端异常 | 内部错误 |

---

## 10. 非功能性需求

### 10.1 性能要求

| 场景 | 目标 |
|---|---|
| 页面首屏加载（有缓存） | < 1s |
| 页面首屏加载（4G 网络，无缓存） | < 2.5s |
| 列表渲染（100 条需求） | < 100ms |
| 状态切换、表单保存等操作反馈 | < 200ms |
| AI 分析响应（本地规则引擎） | < 2s（模拟延迟） |
| AI 分析响应（LLM API） | < 5s |

### 10.2 可用性

- 当前版本为纯前端 SPA，无后端依赖，可用性 = 静态文件服务可用性（GitHub Pages 99.9%+）
- 所有数据本地持久化，网络断开不影响使用

### 10.3 数据安全（生产化时）

| 要求 | 说明 |
|---|---|
| 传输加密 | 全程 HTTPS |
| 认证方式 | JWT（access token 2h 有效期 + refresh token 30 天） |
| Token 存储 | HttpOnly Cookie，防止 XSS |
| 富文本 XSS 防护 | Tiptap 输出内容使用白名单过滤，禁止 script/iframe 等危险标签 |
| API 限流 | 通用接口：60 次/分钟/用户；AI 接口：10 次/分钟/用户 |
| SQL 注入防护 | 使用 ORM（Prisma/Sequelize），禁止拼接 SQL |

### 10.4 浏览器兼容性

| 浏览器 | 最低版本 |
|---|---|
| Chrome | 90+ |
| Safari | 15+ |
| Firefox | 90+ |
| Edge | 90+ |

### 10.5 分辨率支持

| 场景 | 分辨率 |
|---|---|
| 最低支持 | 1280 × 800 |
| 推荐 | 1440px 宽度以上 |
| 移动端 | v1.0 暂不支持，v1.2 规划 |

---

## 11. 技术架构

### 11.1 当前架构（前端 SPA）

```
技术栈：
├── 框架：React 19 + Vite 8
├── 样式：Tailwind CSS v4（@tailwindcss/vite 插件）
├── 状态管理：Zustand 5 + persist 中间件（LocalStorage）
├── 路由：React Router v6（HashRouter，支持 GitHub Pages）
├── 富文本编辑器：Tiptap（@tiptap/react + @tiptap/starter-kit + @tiptap/pm）
├── 图标：Lucide React
├── AI 模块：本地规则引擎（utils/aiEngine.js）
└── 部署：GitHub Pages（gh-pages npm 包）

构建产物：~690KB（gzip: ~211KB）
```

### 11.2 目标架构（前后端分离）

```
前端层：
├── 同上 SPA
├── 网络层：Axios + React Query（缓存 + 自动同步）
└── 部署：CDN 静态托管（阿里云 OSS / Vercel）

后端层：
├── 框架：Node.js（Fastify）或 Python（FastAPI）
├── ORM：Prisma（TypeScript）
├── 数据库：PostgreSQL（主库）+ Redis（会话/缓存）
├── 文件存储：阿里云 OSS / AWS S3（附件上传）
├── AI 服务：Claude API（claude-sonnet-4-6）
└── 部署：Docker 容器化 + Kubernetes 或 Railway

认证服务：
└── JWT + Refresh Token + HttpOnly Cookie
```

### 11.3 目录结构

```
crm-task-sync/
├── public/                   # 静态资源
├── src/
│   ├── components/
│   │   └── Sidebar.jsx       # 全局导航侧边栏
│   ├── pages/
│   │   ├── Dashboard.jsx     # 总览工作台
│   │   ├── Customers.jsx     # 客户管理（含 PRODUCT_CATEGORIES 导出）
│   │   ├── Inbox.jsx         # 需求管理
│   │   ├── Schedule.jsx      # 工作计划
│   │   └── DailyNotes.jsx    # 日常事项
│   ├── store/
│   │   └── useStore.js       # Zustand 全局状态 + LocalStorage 持久化
│   ├── utils/
│   │   └── aiEngine.js       # AI 规则引擎（分析需求 + 总结日常事项）
│   ├── index.css             # 全局样式（含 Tiptap 富文本样式）
│   └── main.jsx              # 应用入口（HashRouter 挂载）
├── index.html
├── vite.config.js
├── package.json
└── PRD.md                    # 本文档
```

---

## 12. 版本规划

### v1.0（已上线）✅

- [x] 客户管理（CRUD、产品分类分组、详情面板、工作计划关联）
- [x] 需求管理（CRUD、7 种状态流转、AI 分析、附件、多维筛选）
- [x] 工作计划（3 Tab 自动聚合：待处理/产品方案/开发计划、进度备注内联编辑）
- [x] 日常事项（Tiptap 富文本编辑器、AI 总结、分类/时间筛选）
- [x] 总览工作台（4 统计卡片、待处理需求列表、进行中需求列表）
- [x] AI 能力（需求分析、日常总结，本地规则引擎实现）

### v1.1（规划中）

- [ ] 用户账号体系（注册/登录，JWT 认证）
- [ ] 数据云端存储（替换 LocalStorage，对接后端 API）
- [ ] AI 接入 Claude API（替换本地规则引擎，提升分析质量）
- [ ] 附件真实上传（对接对象存储）
- [ ] 需求状态变更历史记录

### v1.2（规划中）

- [ ] 多人协作（需求可分配负责人，@提醒）
- [ ] 客户联系人管理（一客户多联系人）
- [ ] 需求批量导出（Excel / PDF）
- [ ] 数据看板（需求趋势折线图、客户需求分布、交付率统计）
- [ ] 移动端响应式适配

### v2.0（远期规划）

- [ ] 与外部系统集成（飞书/钉钉 机器人推送、Jira 需求同步）
- [ ] 微信/企业微信快捷录入（通过 Webhook 接收消息）
- [ ] 智能需求拆解（AI 将大需求拆分为子任务并给出工时估算）
- [ ] 客户健康度评分（基于需求响应速度、交付率等指标）

---

*文档维护：产品团队 | 最后更新：2026-03-30*
